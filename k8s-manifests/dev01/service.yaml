---
apiVersion: v1
kind: Service
metadata:  
  name: dmbc
  labels:
    app: dmbc
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    matchExpressions:
      - {key: app, operator: In, values: [dmbc01, dmbc02, dmbc03, dmbc04]}
  type: ClusterIP